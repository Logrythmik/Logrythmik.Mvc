

namespace Logrythmik.Mvc.ViewModels
{
    /*
    internal class ExampleViewModel : ViewModel<ExampleViewModel, SomeEntity, int, SomeDataContext>
    {
        static ExampleViewModel()
        {
            // View -> Model
            //ViewToModelMap

            // Model -> View
            //ModelToViewMap
            
            // DEFINE SETUP FROM REPOSITORY FOR EXTERNAL DATA
            ExternalDataSetup = (controller, view) => { };
            
            // DEFINE SETUP FROM REQUEST ON POST
            RequestBinding = (request, view) => { };
        }

        #region Constructors
        
        public ExampleViewModel() {}

        public ExampleViewModel(SomeEntity module)
        {
            MapFromModel(module);
        }

        #endregion

        // TODO: Add properties here for view

    }
    */
}
